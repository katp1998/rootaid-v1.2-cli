
const PROJECT_BASE_FOLDER = '/src/projects'

const projects = {
    
}

function generatePath(projectUID){
        return (`${PROJECT_BASE_FOLDER}/`)
    }

//create pure function that matches argument with project combo

generatePath()

